-page_title("Creating asset")

.form_holder
  -form_for( current_object, :url => admin_assets_path ) do |f|
    =render :partial => "form", :locals => { :f => f }
    %p.submit
      =f.submit "Create asset"